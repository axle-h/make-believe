apiVersion: v1
kind: Service
metadata:
  name: make-believe
  labels:
    app: make-believe
spec:
  type: ClusterIP
  ports:
    # Named, so the ingress refers to it by name and never by number.
    - port: 80
      targetPort: http
      name: http
  selector:
    app: make-believe
